function save_feature(index, frm, des, img, p)
%
% David Z, 3/6/2015 
% save features to the index file, directory info is in the global_def 
%

if nargin == 0 % test
    global_def;
    index = 1; 
end

%% get file name  
global g_data_dir g_data_prefix
file_name = sprintf('%s/visual_feature/%s_%04d.mat', g_data_dir, g_data_prefix, index); 

%% to be compatable with soonhac' load and save visual feature function 
elapsed_sift = 0;
elapsed_pre = 0;


end